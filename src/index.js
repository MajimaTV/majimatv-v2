import React from "react";
import ReactDOM from "react-dom/client";
import MajimaTV from "./MajimaTV";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MajimaTV />
  </React.StrictMode>
);
